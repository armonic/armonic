Dump database
-------------

A state Lock (after Active for instance) with provides
  - get_dump
  - get_auth
  - lock

Previous states should have a provide unlock.

Then a provide or state that requires these information should have require like:
RequireExternal( lock )
RequireExternal( get_dump )
RequireExternal( get_auth )
RequireExternal( unlock )


